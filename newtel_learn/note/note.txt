$scope: biến giao tiếp giữa controller và view 
ng-app <=> angular.module(name_app, [])
ng-controller <=> my_app.controller(name_controller, function($rootScope))
    $rootScope.thuoctinh/function = ....
ng-model="" -> binding dữ liệu 
ng-init = ...... -> khai báo trực tiếp ở view
ng-repeat="item in items" -> loop
ng-hide -> ẩn/hiện
ng-include=... -> chèn file html vào 
ng-click=name_function() -> lắng nghe sự kiện

filters: ex: {{thuoc_tinh | upperCase}}
route: 
    + import angular-route-js cdn 
    + ng-view: dl các trang sẽ vào đây
    + app.config
angularjs validation: 

dependance: dùng chung cho nhiều controller
    + value 
    + factory
    + provider 
    + constant
    + service

directives: ng-....




db::transaction
DB::commit 
DB::rollback

angulajs route



tại sao khi lưu localstorete để lưu menu current thì phải f5 lại ms đc ?




cú pháp: 
    1. Không dùng được trait trong controller?
    2. Không lấy được property là array trong ?
logic: 
    3. Giới hạn số request gửi lên serve
    4. ng-model trong editRole nếu cho lên thẻ span thì ko gửi đc index qua trong khi thẻ span vẫn trong repeat?
    5. Nghịch đảo của checked trong hàm parentClick?
    6. api gửt request lên serve(ko kèm theo userId, email, token, jwt), bên serve dùng Auth để check đăng nhập. Auth dựa vào cơ chế nào để biết đc use đn là ai trong khi server chỉ lưu session ?
    7. laravel/sql có cái j hỗ trợ gần giống $facet bên mongodb ko?














brainstorm
flow 
diagrams
lazy loading